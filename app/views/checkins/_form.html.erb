<%= form_with(model: checkin) do |form| %>
  <% if checkin.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(checkin.errors.count, "error") %> prohibited this checkin from being saved:</h2>

      <ul>
        <% checkin.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.label :habit_id, style: "display: block" %>
    <%= form.text_field :habit_id %>
  </div>

  <div>
    <%= form.label :checked_in, style: "display: block" %>
    <%= form.checkbox :checked_in %>
  </div>

  <div>
    <%= form.label :date, style: "display: block" %>
    <%= form.date_field :date %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
